# amap_kit_map_android

`amap_kit_map` 的 Android 官方实现（endorsed implementation），基于高德地图 SDK，最低 Android API 24。应用通过 `amap_kit_map` 自动使用本包，无需直接依赖。

本包持有 Android 专用的 Pigeon schema、生成通道和传输类型，属于实现细节，不对应用导出。
