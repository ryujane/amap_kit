# amap_kit_location_android

`amap_kit_location` 的 Android 官方实现（endorsed implementation），基于高德定位 SDK，支持前台单次与持续定位，最低 Android API 24。应用通过 `amap_kit_location` 自动使用本包，无需直接依赖。

本包持有 Android 专用的 Pigeon schema、生成通道和传输类型
